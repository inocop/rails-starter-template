version: '2'
services:
  db:
    build:
      context: ../dockerfiles/db
      dockerfile: "Dockerfile"
    container_name: "rails_dev_db_1"
    environment: 
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - rails_db:/var/lib/mysql


  web:
    build: 
      context: ../dockerfiles/web
      dockerfile: "Dockerfile_centos7"
      args:
        - RAILS_ENV=development
    container_name: "rails_dev_web_1"
    ports:
      - "8081:80"
      - "1231:1231"
      - "3001:3001"
    volumes:
      - ../../../:/var/my_dir:cached
      - os_log:/var/log
      - node_package:/var/my_dir/app/node_app/node_modules
    privileged: true


  mail: # 開発環境用のメールサーバ
    image: sj26/mailcatcher
    ports:
      - "1081:1080"

volumes:
  rails_db:
    driver: 'local'
  os_log:
    driver: 'local'
  node_package:
    driver: 'local'
